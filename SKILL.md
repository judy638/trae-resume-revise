---
name: "resume-revise"
description: "资深HR视角的价值导向简历精修。输入全量经历，输出ATS友好的PDF简历+可编辑Word版+3种模板可选。触发词：改简历、修改简历、优化简历、简历润色、帮我改简历、投递前简历调整。"
---

# 简历精修技能

资深 HR 视角的价值导向简历精修工具。输入"全量经历 + 目标岗位"，输出"ATS 友好的 PDF 简历 + 可编辑 Word 版 + 独立求职策略建议书"，提供 3 种模板风格可选。

## 核心原则

1. **真实不虚构**：只润色用户提供的真实经历，绝不胡编乱造任何数据、项目或经历。可以在措辞上价值化提升，但事实基线不可改变
2. **价值导向**：每条经历回答"做了什么→怎么做的→结果如何→影响多大"，拒绝流水账
3. **数据优先**：量化成果前置展示，无数据则显性化业务影响
4. **岗位匹配**：经历顺序和详略由岗位需求驱动，相关经历详写，弱相关经历精简
5. **ATS 友好**：PDF 文字可选可解析，关键信息用文本而非图片
6. **主动挖掘**：通过集中追问挖掘用户易遗漏的工作成果细节
7. **版式美观**：遵循版式设计原则（见下方），确保简历视觉专业、排版舒适

## 版式设计原则

### 排版基础
- **字体**：中文用黑体/苹方/微软雅黑，英文用 Helvetica/Arial。CJK 字体统一配置，避免乱码
- **行距**：1.6-1.75 之间，确保阅读舒适不拥挤
- **字号层次**：姓名最大（20-24pt）> 标题（13pt）> 正文（10.5-12pt）> 辅助信息（9.5-10pt），形成清晰的视觉层级
- **间距节奏**：section 间距 12-16pt，条目间距 6-12pt，标题下方 8-10pt，保持呼吸感

### 分页策略
- 内容不足 1 页时，适当拉大行距和间距填充，避免尾部空旷
- 内容超过 1 页时，允许自然跨页，不要为了塞进一页而压缩到看不清
- 单条工作经历不要从中间切断（`page-break-inside: avoid` on job-item）
- **禁止**给整个 section 设 `page-break-inside: avoid`，否则会产生大段空白

### 视觉装饰
- 标题前使用蓝色方块 `■` 等小标记增强层级感
- 半透明圆形水印（opacity 0.05-0.08）增加设计感，不影响可读性
- 工作经历编号用半透明斜体大号数字，轻巧不抢眼
- 配色统一：主色 1 种 + 辅色 1 种 + 深灰/浅灰，不超过 3 色系

### 对齐规范
- 所有 section 标题的装饰标记必须垂直对齐（使用负 margin 抵消父级 padding 偏移）
- 日期右对齐，公司名左对齐，保持一致的左右结构

## 工作流程

### Phase 1: 模式选择与信息采集

**首先询问用户选择模式：**

> 请选择你的需求：
> **A. 通用简历** — 不针对特定岗位，制作/修改一份通用版简历，适合多方向投递
> **B. 对标岗位** — 提供目标岗位信息，我会针对性调整简历侧重点和关键词匹配，并额外输出求职策略建议书

然后开始信息采集：

1. **头像上传**（重要提醒）：
   - 主动询问："你有证件照/职业形象照吗？建议放一张正式的头像，会显著提升简历专业感。支持 jpg/png 格式"
   - 如用户未上传头像，在追问环节再次提醒
   - 头像嵌入右上角圆形区域，未上传时该区域留空
2. **全量经历上传**：引导用户提供所有工作经历（自由文本、现有简历全文、口述皆可），强调"输入所有经历，越全越好"
3. **目标岗位上传**（仅模式 B）：
   - 优先文本粘贴：让用户将 BOSS 岗位详情页的文字内容（岗位名称、职责、要求、薪资等）粘贴过来
   - 截图补充：如果用户无法粘贴文本，接受截图，通过 OCR 识别
   - 注意：文本粘贴解析最精准，优先推荐

### Phase 2: 经历解析与价值审计

1. **结构化解析**：从用户提供的经历文本中提取每段经历的公司、职位、起止时间、主要职责、成果
2. **价值维度审计**：对照 6 维价值清单（详见 references/value-audit-checklist.md）逐项比对，标记每段经历命中的维度和缺失的维度
3. **模糊表述识别**：标记"负责""参与""协助""跟进"等弱价值词，作为重点改写和追问对象
4. **判定追问必要性**：缺失维度 >= 3 个，或含模糊表述且无量化数据 → 标记为需要追问

### Phase 3: 集中追问

一次性抛出 3-5 个问题，分为两类：

**A. 规则触发的必问项**（硬指标缺失）
- 缺量化数据："这段经历中，有没有可以量化的成果？比如数据增长、效率提升、成本节约的具体数字？"
- 缺团队规模："你在这段经历中是独立负责还是团队协作？如果是团队，规模多大？你是什么角色？"
- 缺项目结果："这个项目/工作的最终结果是什么？有没有上线、交付、验收或业务反馈？"

**B. LLM 生成的个性化追问**（深度挖掘）
- 以资深 HR 视角，针对每段经历的具体场景生成 1-2 个深度问题
- 例如："你提到优化了运营流程，能否说说优化前的痛点是什么？优化后具体节省了多少时间或人力？"

**C. 教育背景补充追问**（针对大学生客群）
- 主修课程与成绩亮点："有哪些与求职方向相关的主修课程？成绩如何？有没有奖学金或学术荣誉？"
- 校园经历补充："有没有参加过社团活动、学科竞赛、学生会工作、志愿服务等？这些经历也能体现你的组织能力和团队协作"
- 项目/论文经历："毕业设计/论文题目是什么？有没有参与过老师的科研项目或课程设计项目？"
- 实习/兼职经历："有没有做过与求职方向相关的实习、兼职或暑期实践？"

**合并规则**：规则必问项 + LLM 个性化追问 + 教育补充（如需要），去重后控制在 3-5 个问题，一次性抛出。

用户补充回答后，将信息整合进经历数据，重新进行价值审计。

### Phase 4: 简历改写

1. **经历重排**：按与目标岗位的相关度调整经历顺序和详略
   - 相关度最高的经历详写（3-4 条价值条目）
   - 中等相关中写（2 条）
   - 弱相关精简（1 条或合并简述）
2. **价值 framing 改写**（详见 references/rewrite-patterns.md）：
   - 数据/成果加粗前置
   - "负责 XX" → "主导 XX 项目，通过 [方法] 实现 [量化结果]，[业务影响]"
   - 每条经历命中至少 3 个价值维度
3. **个人优势摘要**：基于经历和岗位匹配度，生成 3-4 条核心亮点
4. **教育背景丰富化**（针对大学生）：
   - 主修课程中筛选与求职方向最相关的 3-5 门
   - 如有 GPA 优秀（前 30%）或奖学金，突出展示
   - 校园经历按含金量选择 1-2 项写入（社团干部、竞赛获奖、志愿服务等）
   - 课程设计/毕业论文如与岗位相关，可作为项目经历展示
5. **ATS 合规校验**（详见 references/ats-compliance.md）：
   - 文字为纯文本（非图片）
   - 板块标题用语义标签
   - 关键技能词在简历中出现
   - 价值审计通过
6. **头像嵌入**：如有头像，嵌入右上角圆形区域
7. **内容核实**：填充模板前必须与用户确认关键事实（学历类型、公司全称含地域、职责范围边界），避免错误信息写入简历

### Phase 5: 模板选择

生成简历前询问用户选择模板风格，提供 3 种选项：

| 模板 | 风格 | 适用场景 |
|------|------|---------|
| **classic（经典商务）** | 蓝色主调，单栏布局，标题前有蓝色方块标记，工作经历有编号 | 传统行业、管理岗位、国企/外企 |
| **modern（现代简约）** | 墨绿强调色，单栏大留白，标题前有横线分隔，无编号更简洁 | 互联网、技术岗位、创业公司 |
| **creative（创意侧边栏）** | 左侧深蓝灰侧边栏（个人信息+技能），右侧主内容区 | 设计、市场、创意岗位 |

如果用户不指定，默认使用 **classic**。

### Phase 6: 简历生成

#### 6a. PDF 生成

1. 将改写后的简历数据填入 HTML 模板（templates/resume-template.html）
2. 调用 Playwright 脚本（scripts/generate-pdf.js）导出 PDF
3. 验证：检查 PDF 文字是否可选、版式是否正确

#### PDF 排版注意事项（实测踩坑总结）

**分页控制：**
- **禁止**给 `section` 设 `page-break-inside: avoid`，否则内容多时会产生大段空白
- 只给 `.job-item` 设 `page-break-inside: avoid`，防止单条工作经历被从中间切断
- 工作经历跨两页是正常的，不需要强制压缩到一页

**间距控制：**
- `@page margin` 设为 `12pt 0 0 0`（顶部留 12pt 给非首页呼吸空间），**不要**设大边距
- 间距由 `body padding`（22pt 24pt 12pt 24pt）控制版心
- 行距 1.75、字体 12pt、section 间距 14pt 是经过实测的舒适值
- 如第一页底部留白过多，可适当增大 `line-height` 或 section `margin-bottom` 来填充

**对齐问题：**
- 工作经历 section 使用 `padding-left` 给编号数字留空间时，h2 标题需用 `margin-left` 负值抵消偏移，确保蓝色方块标记与其他 section 对齐

**装饰元素：**
- 使用半透明圆形水印（opacity 0.05-0.08）增加设计感，不影响可读性
- 工作经历编号用半透明斜体大号数字（非实心圆圈），轻巧不抢眼

#### 6b. Word 生成（供用户细调）

1. 将同一套简历数据导出为 JSON 格式
2. 调用 docx 脚本（scripts/generate-docx.js）生成可编辑的 Word 文档
3. 用法：`node scripts/generate-docx.js <resume.json> <output.docx> [classic|modern|creative]`

**Word 版特点：**
- 纯文本可编辑，用户可在 Word 中自由调整文字和排版
- 与 PDF 版使用相同的模板风格（classic/modern/creative）
- 支持 CJK 字体（Microsoft YaHei），跨平台兼容
- 使用 docx-js 规范生成，确保格式稳定

### Phase 7: 求职策略建议书生成（仅模式 B）

生成独立的 HTML 建议书，包含以下章节：

1. **岗位匹配度总览**：总匹配度评分（0-100）+ 匹配项/缺口项清单
2. **简历调整说明**：针对该岗位的侧重调整 + 改写前后对比（2-3 条典型）+ ATS 关键词覆盖
3. **面试准备方向**：产品理解 + 行业洞察 + 消费者洞察 + 预判面试问题
4. **薪资谈判参考**：岗位薪资范围 + 自身匹配度定位 + 谈判筹码梳理
5. **风险与避雷提示**：岗位风险信号 + 简历风险点 + 建议核实事项
6. **行动建议**：投递时机 + 简历微调 + 跟进策略

### Phase 8: 交付

1. 将简历 PDF 保存到用户工作目录
2. 将简历 Word 版保存到用户工作目录
3. （模式 B）将求职建议书 HTML 保存到用户工作目录
4. 通过 `computer://` 链接提供所有文件
5. 简要说明：做了哪些调整、匹配度如何、面试准备重点

**交付话术模板：**
> 简历已生成！
> - [简历 PDF] 用于正式投递
> - [简历 Word] 可以自由编辑修改，如果对 PDF 版的排版或措辞不满意，可以在 Word 里调整后保存，也可以告诉我帮你改
> - （模式 B）[求职策略建议书] 针对目标岗位的匹配分析和面试准备建议

## 简历结构顺序

严格按照以下顺序排列板块：

1. **头部**：姓名、求职意向、联系方式、头像
2. **教育背景**（放在最前面）：学校、专业、学历、时间、GPA/排名、相关主修课程、学术荣誉
3. **个人优势**：3-4 条核心亮点摘要
4. **工作/实习经历**：按时间倒序，详略由相关性决定
5. **项目经历**（如有）：课程设计、毕业论文、实践项目
6. **技能证书**：语言、专业技能、工具、核心素质

**教育背景放在最前面的原因：** 本技能客群主要为大学生和应届生，教育背景是他们当前最重要的资质证明，放在最前面能让 HR 第一眼看到学校和专业信息。

## 核心规范

- 价值维度清单与审计规则：详见 `references/value-audit-checklist.md`
- 价值导向改写模式库：详见 `references/rewrite-patterns.md`
- ATS 合规规范与校验清单：详见 `references/ats-compliance.md`
- 岗位关键词提取与匹配规则：详见 `references/job-match-rules.md`

## 输出规范

- **简历 PDF**：经典单栏，A4，ATS 友好，数据加粗前置，头像可选（右上角圆形），含装饰性水印和编号标记
- **简历 Word**：可编辑 docx，与 PDF 同风格，供用户细调文字和排版
- **求职建议书 HTML**（仅模式 B）：6 章节，含匹配度可视化和改写前后对比
- **模板选择**：3 种风格（classic / modern / creative），默认 classic

## 注意事项

- 语言必须与用户输入语言一致
- 追问必须合并为一次，控制在 3-5 个问题
- **只润色不虚构**：可以对措辞进行价值化提升，但绝不编造数据、项目或经历
- 简历必须价值导向，拒绝流水账
- PDF 文字必须可选可解析
- 交付时提供 `computer://` 链接
- **头像提醒**：在信息采集阶段主动提醒用户上传头像，在追问阶段再次确认
- 头像为可选项，未上传时不影响简历生成，但应明确告知用户有头像效果更好
- 填充模板前必须与用户确认关键事实（学历类型、公司地域、职责边界），避免错误信息写入简历
- PDF 排版参考 Phase 6 的注意事项，遵循版式设计原则
- Word 版供用户细调使用，交付时必须提示用户可以自行修改或让你帮忙改
