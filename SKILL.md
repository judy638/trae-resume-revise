---
name: "resume-revise"
description: "资深HR视角的价值导向简历精修。输入全量经历，输出ATS友好的PDF简历，通过多轮追问持续迭代完善。触发词：改简历、修改简历、优化简历、简历润色、帮我改简历、投递前简历调整。"
---

# 简历精修技能

资深 HR 视角的价值导向简历精修工具。输入"全量经历"，通过多轮追问持续迭代，输出"ATS 友好的 PDF 简历"。

## 核心原则

1. **价值导向**：每条经历回答"做了什么→怎么做的→结果如何→影响多大"，拒绝流水账
2. **数据优先**：量化成果前置展示，无数据则显性化业务影响
3. **持续迭代**：通过多轮追问不断完善简历内容，每次追问 3-5 个问题，用户回答后重新审计和改写
4. **ATS 友好**：PDF 文字可选可解析，关键信息用文本而非图片
5. **主动挖掘**：通过集中追问挖掘用户易遗漏的工作成果细节

## 工作流程

### Phase 1: 信息采集

1. **头像上传**（可选）：询问用户是否提供头像图片，支持 jpg/png/webp 格式，提供本地文件路径
2. **全量经历上传**：引导用户提供所有工作经历（自由文本、现有简历全文、口述皆可），强调"输入所有经历，越全越好"
3. **求职意向确认**（可选）：询问用户的求职意向（目标岗位、期望城市、薪资范围等），作为简历撰写的参考信息，但不作为必要输入

### Phase 2: 经历解析与价值审计

1. **结构化解析**：从用户提供的经历文本中提取每段经历的公司、职位、起止时间、主要职责、成果
2. **价值维度审计**：对照 6 维价值清单（详见 references/value-audit-checklist.md）逐项比对，标记每段经历命中的维度和缺失的维度
3. **模糊表述识别**：标记"负责""参与""协助""跟进"等弱价值词，作为重点改写和追问对象
4. **判定追问必要性**：缺失维度 >= 3 个，或含模糊表述且无量化数据 → 标记为需要追问

### Phase 3: 集中追问

一次性抛出 3-5 个问题，分为两类：

**A. 规则触发的必问项**（硬指标缺失）
- 缺量化数据："这段经历中，有没有可以量化的成果？比如数据增长、效率提升、成本节约的具体数字？"
- 缺团队规模："你在这段经历中是独立负责还是团队协作？如果是团队，规模多大？你是什么角色？"
- 缺项目结果："这个项目/工作的最终结果是什么？有没有上线、交付、验收或业务反馈？"

**B. LLM 生成的个性化追问**（深度挖掘）
- 以资深 HR 视角，针对每段经历的具体场景生成 1-2 个深度问题
- 例如："你提到优化了运营流程，能否说说优化前的痛点是什么？优化后具体节省了多少时间或人力？"

**合并规则**：规则必问项 + LLM 个性化追问，去重后控制在 3-5 个问题，一次性抛出。

用户补充回答后，将信息整合进经历数据，重新进行价值审计。

**追问可多轮进行**：如果审计发现信息仍不充分，继续追问。每轮追问 3-5 个问题，直到简历质量达到较高水平。

### Phase 4: 简历改写

1. **经历排序**：按时间倒序排列，同时间段内按含金量（成果影响力和数据表现）从高到低排序
   - 含金量最高的经历详写（3-4 条价值条目）
   - 中等含金量中写（2 条）
   - 含金量较低精简（1 条或合并简述）
2. **价值 framing 改写**（详见 references/rewrite-patterns.md）：
   - 数据/成果加粗前置
   - "负责 XX" → "主导 XX 项目，通过 [方法] 实现 [量化结果]，[业务影响]"
   - 每条经历命中至少 3 个价值维度
3. **个人优势摘要**：基于经历生成 3-4 条核心亮点，突出用户的综合实力和差异化优势
4. **ATS 合规校验**（详见 references/ats-compliance.md）：
   - 文字为纯文本（非图片）
   - 板块标题用语义标签
   - 价值审计通过
5. **头像嵌入**：如有头像，嵌入右上角圆形区域
6. **内容核实**：填充模板前必须与用户确认关键事实（学历类型、公司全称含地域、职责范围边界），避免错误信息写入简历

### Phase 5: PDF 生成

1. 将改写后的简历数据填入 HTML 模板（templates/resume-template.html）
2. 调用 Playwright 脚本（scripts/generate-pdf.js）导出 PDF
3. **分页自适应逻辑**：
   - 根据内容总量自动判断是否需要分页
   - 内容适合单页（如在校生简历）：尽量压缩字体、行距、间距，控制在单页内
   - 内容超过单页：增大行距和间距让内容自然填充，确保第二页内容量充足（至少占页面 60%），避免半页空白
4. 验证：检查 PDF 文字是否可选、版式是否正确、分页是否合理

#### PDF 排版注意事项（实测踩坑总结）

**分页控制：**
- 给 `.job-item` 设 `page-break-inside: avoid`，防止单条经历被从中间切断
- **不要**给 `section` 设 `page-break-inside: avoid`
- 如果内容总量适合单页（如在校生简历），尽量压缩到单页
- 如果必须两页，确保第二页内容量充足（至少占页面 60%），不要出现半页空白
- 分页点应选择在两个 section 之间或两个 job-item 之间，不要在 section 内部分页

**间距控制：**
- `@page margin` 设为 `10pt 0 0 0`
- 间距由 `body padding`（18pt 20pt 10pt 20pt）控制版心
- 默认行距 1.6、字体 11pt
- 如果内容超过 1 页，增大行距和间距让内容自然填充
- 如果内容不足 1 页，适当压缩行距和间距让内容紧凑但不过度拥挤

**头部区域：**
- `resume-header` 使用 `margin-bottom: 14pt` 而非 `0`，确保与下方内容有足够呼吸空间
- 蓝色下划线与教育背景标题之间至少保留 14pt 间距

**装饰元素：**
- 使用半透明圆形水印（opacity 0.05-0.08）增加设计感，不影响可读性
- 工作经历编号用半透明斜体大号数字

**内容核实：**
- 填充模板前务必与用户确认关键信息准确性（学历类型、公司全称含地域、职责范围等）
- 容易出错的信息：辅修 vs 分流方向、公司地域前缀、"主导" vs "参与"的准确程度

### Phase 6: 交付与迭代

1. 将简历 PDF 保存到用户工作目录
2. 通过 `computer://` 链接提供 PDF 文件
3. 简要说明：做了哪些调整、简历亮点

**迭代优化：**
- 如果用户对简历内容有修改意见或补充信息，根据反馈类型决定：
  - 信息补充类反馈 → 回到 Phase 3 继续追问，收集更多细节后重新审计和改写
  - 排版/措辞类反馈 → 回到 Phase 4 直接修改后重新生成 PDF
- 每次迭代后重新交付 PDF，用户可以继续提出修改意见
- 迭代可持续进行，直到用户满意

## 核心规范

- 价值维度清单与审计规则：详见 `references/value-audit-checklist.md`
- 价值导向改写模式库：详见 `references/rewrite-patterns.md`
- ATS 合规规范与校验清单：详见 `references/ats-compliance.md`

## 输出规范

- **简历 PDF**：经典单栏，A4，ATS 友好，数据加粗前置，头像可选（右上角圆形），含装饰性水印和编号标记
- **分页自适应**：根据内容量自动调整字体大小、行距、间距，避免第一页拥挤 + 第二页空白
- **只输出 PDF**，不输出 HTML 建议书

## 注意事项

- 语言必须与用户输入语言一致
- 追问必须合并为一次，控制在 3-5 个问题
- 追问可以多轮进行，直到简历质量达到较高水平
- 每次改写后交付 PDF，用户可以继续提出修改意见
- 简历必须价值导向，拒绝流水账
- PDF 文字必须可选可解析
- 交付时提供 `computer://` 链接
- 头像为可选项，未上传时不影响简历生成
- 不需要目标岗位即可改简历，求职意向仅为可选参考
- 填充模板前必须与用户确认关键事实（学历类型、公司地域、职责边界），避免错误信息写入简历
- PDF 排版参考 Phase 5 的注意事项，避免分页空白和对齐问题
