---
name: "resume-revise"
description: "资深HR视角的价值导向简历精修。输入全量经历，输出ATS友好的PDF简历+可编辑Word版+3种模板可选+含天眼查公司调研的Tab导航求职策略建议书。触发词：改简历、修改简历、优化简历、简历润色、帮我改简历、投递前简历调整。"
---

# 简历精修技能

资深 HR 视角的价值导向简历精修工具。输入"全量经历 + 目标岗位"，输出"ATS 友好的 PDF 简历 + 可编辑 Word 版 + 含天眼查公司调研的 Tab 导航求职策略建议书"，提供 3 种模板风格可选。

## 核心原则

1. **真实不虚构**：只润色用户提供的真实经历，绝不胡编乱造任何数据、项目或经历。可以在措辞上价值化提升，但事实基线不可改变
2. **价值导向**：每条经历回答"做了什么→怎么做的→结果如何→影响多大"，拒绝流水账
3. **数据优先**：量化成果前置展示，无数据则显性化业务影响
4. **岗位匹配**：经历顺序和详略由岗位需求驱动，相关经历详写，弱相关经历精简
5. **ATS 友好**：PDF 文字可选可解析，关键信息用文本而非图片
6. **主动挖掘**：通过集中追问挖掘用户易遗漏的工作成果细节
7. **版式美观**：遵循版式设计原则（见下方），确保简历视觉专业、排版舒适
8. **选择框交互**：所有需要用户做选择的环节，必须使用 `AskUserQuestion` 工具呈现选择框，禁止让用户输入字母/数字/文字回复

## 版式设计原则

### 排版基础
- **字体**：中文用黑体/苹方/微软雅黑，英文用 Helvetica/Arial。CJK 字体统一配置，避免乱码
- **行距**：1.6-1.75 之间，确保阅读舒适不拥挤
- **字号层次**：姓名最大（20-24pt）> 标题（13pt）> 正文（10.5-12pt）> 辅助信息（9.5-10pt），形成清晰的视觉层级
- **间距节奏**：section 间距 12-16pt，条目间距 6-12pt，标题下方 8-10pt，保持呼吸感

### 分页策略
- 内容不足 1 页时，适当拉大行距和间距填充，避免尾部空旷
- 内容超过 1 页时，允许自然跨页，不要为了塞进一页而压缩到看不清
- 单条工作经历不要从中间切断（`page-break-inside: avoid` on job-item）
- **禁止**给整个 section 设 `page-break-inside: avoid`，否则会产生大段空白

### 视觉装饰
- 标题前使用蓝色方块 `■` 等小标记增强层级感
- 半透明圆形水印（opacity 0.05-0.08）增加设计感，不影响可读性
- 工作经历编号用半透明斜体大号数字，轻巧不抢眼
- 配色统一：主色 1 种 + 辅色 1 种 + 深灰/浅灰，不超过 3 色系

### 对齐规范
- 所有 section 标题的装饰标记必须垂直对齐（使用负 margin 抵消父级 padding 偏移）
- 日期右对齐，公司名左对齐，保持一致的左右结构

## 工作流程

### Phase 1: 简历上传与模式选择

**第一步：请用户上传简历（首先动作）**

对话一开始，主动引导用户上传现有简历，并明确告知简历应包含的内容模块：
- "请将你的简历发给我（PDF/Word/图片/纯文本均可）。如果还没有成型的简历，也可以把你的经历信息以文字形式发给我。"
- **提示用户简历应包含以下内容**（如有遗漏可提醒补充）：
  - **基本信息**：姓名、手机号、邮箱、所在城市
  - **教育背景**：学校名称、专业、学历层次（本科/硕士等）、就读时间、GPA/排名（如优秀可写）、主修课程（选填）
  - **工作/实习经历**：公司名称、职位、起止时间、主要职责和成果（尽量带数据）
  - **项目经历**（如有）：项目名称、角色、做了什么、结果如何
  - **技能证书**：语言能力（如 CET-4/6、专四专八）、专业技能/工具（如 Python、PS、Excel）、职业证书
  - **校园经历**（应届生选填）：社团/学生会职务、竞赛获奖、志愿服务等
  - **头像照片**（选填）：证件照或职业形象照，jpg/png 均可，建议附上
- 等待用户上传简历文件或经历素材后再进入第二步

**第二步：通过 `AskUserQuestion` 工具让用户选择模式（必须用选择框，禁止让用户输入 A/B 或文字）**

用户上传简历后，调用 `AskUserQuestion`，设置如下问题（header: "简历模式"，单选）：

| 选项 | label | description |
|------|-------|-------------|
| 选项 1 | 通用简历 | 你有大概的职业方向，或准备海投多类岗位。制作一份通用版简历，突出核心竞争力，适配多个方向投递 |
| 选项 2 | 对标特定岗位 | 你有明确的目标岗位（如"字节跳动-电商运营"）。针对性调整简历侧重点和关键词匹配，并额外输出求职策略建议书 |

**选择框交互规范：**
- 必须使用 `AskUserQuestion` 工具呈现选择框，禁止用纯文本让用户回复字母或数字
- 每个选项的 `label` 简短（1-5 词），`description` 清晰说明适用场景和后续流程
- 通用简历 = 有大概方向/想海投，不针对某一具体岗位；对标特定岗位 = 有明确的目标岗位名称和公司，需要针对性优化。两个选项的区别必须在 description 中写清楚，避免用户误解
- 用户选完模式后，再进入详细经历采集环节（不要在选模式时同时问经历）

**第三步：详细经历补充（用户选完模式后进行）**

1. **头像上传**（重要提醒）：
   - 主动询问："你有证件照/职业形象照吗？建议放一张正式的头像，会显著提升简历专业感。支持 jpg/png 格式"
   - 如用户未上传头像，在追问环节再次提醒
   - 头像嵌入右上角圆形区域，未上传时该区域留空
2. **经历补充确认**：基于用户上传的简历，提取已有经历并确认是否完整。如用户还有未写入简历的经历，引导其补充。强调"输入所有经历，越全越好"
3. **目标岗位上传**（仅模式 2）：
   - 优先文本粘贴：让用户将 BOSS 岗位详情页的文字内容（岗位名称、职责、要求、薪资、公司名称等）粘贴过来
   - 截图补充：如果用户无法粘贴文本，接受截图，通过 OCR 识别
   - 注意：文本粘贴解析最精准，优先推荐

### Phase 2: 经历解析与价值审计

1. **结构化解析**：从用户提供的经历文本中提取每段经历的公司、职位、起止时间、主要职责、成果
2. **价值维度审计**：对照 6 维价值清单（详见 references/value-audit-checklist.md）逐项比对，标记每段经历命中的维度和缺失的维度
3. **模糊表述识别**：标记"负责""参与""协助""跟进"等弱价值词，作为重点改写和追问对象
4. **判定追问必要性**：缺失维度 >= 3 个，或含模糊表述且无量化数据 → 标记为需要追问
5. **琐碎经历整合触发判定**（新增）：
   - **触发条件**（满足任一即触发）：
     - 单条经历描述不足 15 字，且多条经历描述零散琐碎
     - 同一公司/项目下有 3 条以上碎片化描述，缺乏主线
     - 经历数量超过 8 条，但多数为 1-2 句的简短描述
     - 多条经历内容高度重复或可合并为同一价值主题
   - **触发后流程**：
     1. 暂停改写，通过 `AskUserQuestion` 工具询问用户是否整合（header: "经历整合"，单选）：
        - 选项 1 label: "整合经历" → description: "通过价值分析合并琐碎经历，提炼强重点经历，让简历更有冲击力"
        - 选项 2 label: "保持原样" → description: "保留每条经历独立呈现，我按原结构精修"
     2. 如用户选择"整合经历"：进行价值分析整合
        - 按价值主题聚类：将同类型工作成果归并为一条强经历
        - 提炼主线：找出跨经历的价值主线（如"从 0 到 1 搭建""跨部门协作推动"）
        - 量化汇总：合并同类项的数据（如 3 个项目累计服务 XX 用户）
        - 整合后呈现给用户确认，标注"整合前 X 条 → 整合后 Y 条"
     3. 如用户选择"保持原样"：按原结构进入 Phase 3
     4. 用户确认整合结果后，进入 Phase 3

### Phase 3: 集中追问

一次性抛出 3-5 个问题，分为两类：

**A. 规则触发的必问项**（硬指标缺失）
- 缺量化数据："这段经历中，有没有可以量化的成果？比如数据增长、效率提升、成本节约的具体数字？"
- 缺团队规模："你在这段经历中是独立负责还是团队协作？如果是团队，规模多大？你是什么角色？"
- 缺项目结果："这个项目/工作的最终结果是什么？有没有上线、交付、验收或业务反馈？"

**B. LLM 生成的个性化追问**（深度挖掘）
- 以资深 HR 视角，针对每段经历的具体场景生成 1-2 个深度问题
- 例如："你提到优化了运营流程，能否说说优化前的痛点是什么？优化后具体节省了多少时间或人力？"

**C. 教育背景补充追问**（针对大学生客群）
- 主修课程与成绩亮点："有哪些与求职方向相关的主修课程？成绩如何？有没有奖学金或学术荣誉？"
- 校园经历补充："有没有参加过社团活动、学科竞赛、学生会工作、志愿服务等？这些经历也能体现你的组织能力和团队协作"
- 项目/论文经历："毕业设计/论文题目是什么？有没有参与过老师的科研项目或课程设计项目？"
- 实习/兼职经历："有没有做过与求职方向相关的实习、兼职或暑期实践？"

**合并规则**：规则必问项 + LLM 个性化追问 + 教育补充（如需要），去重后控制在 3-5 个问题，一次性抛出。

用户补充回答后，将信息整合进经历数据，重新进行价值审计。

### Phase 4: 简历改写

1. **经历重排**：按与目标岗位的相关度调整经历顺序和详略
   - 相关度最高的经历详写（3-4 条价值条目）
   - 中等相关中写（2 条）
   - 弱相关精简（1 条或合并简述）
2. **价值 framing 改写**（详见 references/rewrite-patterns.md）：
   - 数据/成果加粗前置
   - "负责 XX" → "主导 XX 项目，通过 [方法] 实现 [量化结果]，[业务影响]"
   - 每条经历命中至少 3 个价值维度
3. **个人优势摘要**：基于经历和岗位匹配度，生成 3-4 条核心亮点
4. **教育背景丰富化**（针对大学生）：
   - 主修课程中筛选与求职方向最相关的 3-5 门
   - 如有 GPA 优秀（前 30%）或奖学金，突出展示
   - 校园经历按含金量选择 1-2 项写入（社团干部、竞赛获奖、志愿服务等）
   - 课程设计/毕业论文如与岗位相关，可作为项目经历展示
5. **ATS 合规校验**（详见 references/ats-compliance.md）：
   - 文字为纯文本（非图片）
   - 板块标题用语义标签
   - 关键技能词在简历中出现
   - 价值审计通过
6. **头像嵌入**：如有头像，嵌入右上角圆形区域
7. **内容核实**：填充模板前必须与用户确认关键事实（学历类型、公司全称含地域、职责范围边界），避免错误信息写入简历
8. **个人信息完整性校验**（防占位符残留）：填充模板前必须逐一检查以下字段是否为真实值（非占位符）：
   - **必填字段**：姓名、手机号、邮箱、学校名称、专业、公司名称、起止时间
   - **校验规则**：
     - 扫描简历数据中是否存在 `{{...}}` 占位符、`xxxx`、`XXX`、`example`、`placeholder`、`你的`、`填写` 等占位标记
     - 手机号必须为 11 位数字（`1[3-9]\d{9}`），不接受 `138xxxx8888` 等脱敏格式
     - 邮箱必须包含 `@` 和域名，不接受 `email.com` 等占位域名
     - 如发现占位符或缺失字段：**立即暂停生成**，通过对话向用户索取真实值，禁止用占位符先生成再让用户自行替换
     - 索取话术示例："你的手机号和邮箱目前还是占位符，请提供真实联系方式，我直接填入简历生成 PDF"
   - **用户拒绝提供时的降级方案**：如用户明确表示不愿提供（如"先用假的"），在简历中该字段处留空（`<span></span>`），**禁止**写入 `138xxxx8888` 等假数据，并告知用户"生成后可自行在 Word 版中填写"

### Phase 5: 模板选择

**生成简历前，通过 `AskUserQuestion` 工具让用户选择模板（必须用选择框，禁止文字输入）：**

调用 `AskUserQuestion`，设置如下问题（header: "模板风格"，单选）：

| 选项 | label | description |
|------|-------|-------------|
| 选项 1 | 经典商务 | 蓝色主调，单栏布局，标题前有蓝色方块标记，工作经历有编号。适用传统行业、管理岗位、国企/外企 |
| 选项 2 | 现代简约 | 墨绿强调色，单栏大留白，标题前有横线分隔，无编号更简洁。适用互联网、技术岗位、创业公司 |
| 选项 3 | 创意侧边栏 | 左侧深蓝灰侧边栏（个人信息+技能），右侧主内容区。适用设计、市场、创意岗位 |
| 选项 4 | 三套全生成 | 一次性生成 classic + modern + creative 三种风格，方便对比挑选最满意的版本 |

**选择框交互规范：**
- 必须使用 `AskUserQuestion` 工具，禁止用纯文本让用户回复字母或数字
- 如用户选择"三套全生成"：依次用 3 种模板各生成一套 PDF + Word，交付时打包提供，并说明各套风格差异
- 如用户不指定或选择困难：默认使用 classic，但必须先呈现选择框让用户决策

### Phase 6: 简历生成

#### 6a. PDF 生成

0. **生成前校验**（必须执行）：遍历简历数据所有字段，确认 Phase 4 step 8 的个人信息完整性校验已通过。如发现任何占位符或缺失字段，**立即暂停**返回 Phase 4 补全，禁止带占位符生成 PDF
1. 将改写后的简历数据填入 HTML 模板（templates/resume-template.html）
2. 调用 Playwright 脚本（scripts/generate-pdf.js）导出 PDF
3. 验证：检查 PDF 文字是否可选、版式是否正确
4. **个人信息回查**：打开生成的 PDF/HTML，grep 检查是否残留 `{{`、`xxxx`、`placeholder` 等占位符，如有则修复后重新生成

#### PDF 排版注意事项（实测踩坑总结）

**分页控制：**
- **禁止**给 `section` 设 `page-break-inside: avoid`，否则内容多时会产生大段空白
- 只给 `.job-item` 设 `page-break-inside: avoid`，防止单条工作经历被从中间切断
- 工作经历跨两页是正常的，不需要强制压缩到一页

**间距控制：**
- `@page margin` 设为 `12pt 0 0 0`（顶部留 12pt 给非首页呼吸空间），**不要**设大边距
- 间距由 `body padding`（22pt 24pt 12pt 24pt）控制版心
- 行距 1.75、字体 12pt、section 间距 14pt 是经过实测的舒适值
- 如第一页底部留白过多，可适当增大 `line-height` 或 section `margin-bottom` 来填充

**对齐问题：**
- 工作经历 section 使用 `padding-left` 给编号数字留空间时，h2 标题需用 `margin-left` 负值抵消偏移，确保蓝色方块标记与其他 section 对齐

**装饰元素：**
- 使用半透明圆形水印（opacity 0.05-0.08）增加设计感，不影响可读性
- 工作经历编号用半透明斜体大号数字（非实心圆圈），轻巧不抢眼

#### 6b. Word 生成（供用户细调）

1. 将同一套简历数据导出为 JSON 格式
2. 调用 docx 脚本（scripts/generate-docx.js）生成可编辑的 Word 文档
3. 用法：`node scripts/generate-docx.js <resume.json> <output.docx> [classic|modern|creative]`

**Word 版特点：**
- 纯文本可编辑，用户可在 Word 中自由调整文字和排版
- 与 PDF 版使用相同的模板风格（classic/modern/creative）
- 支持 CJK 字体（Microsoft YaHei），跨平台兼容
- 使用 docx-js 规范生成，确保格式稳定

#### 6c. 三套全生成模式（用户选择"三套全生成"时执行）

1. 依次用 classic、modern、creative 三种模板各生成一套 PDF + Word
2. 文件命名加后缀区分：`resume-classic.pdf`、`resume-modern.pdf`、`resume-creative.pdf`（Word 同理）
3. 交付时通过 `computer://` 链接提供全部 6 个文件
4. 简要说明三套风格差异，帮助用户对比挑选

### Phase 7: 求职策略建议书生成（所有模式）

**并行生成规则**（缩短用户等待时间）：
- 简历 PDF/Word 生成（Phase 6）与建议书数据采集/HTML 生成（Phase 7）**应并行执行**
- 具体操作：简历生成脚本启动后，立即启动建议书的 WebSearch 数据采集和 HTML 模板填充，不等简历生成完毕再开始建议书
- 如使用 subagent：可将简历生成和建议书生成分别派发给两个独立 subagent 并行处理
- 交付时先交付简历（用户最急需），建议书稍后交付

**两种建议书类型：**
- **模式 1（通用简历）**：生成**职业规划建议书**（Tab 导航 HTML），包含 1-3 个职业推荐方向，每个方向含职业路径、薪资水平、行业前景、核心技能提升清单。模板：`templates/career-advice-template.html`
- **模式 2（对标岗位）**：生成**求职策略建议书**（Tab 导航 HTML），8 个板块含天眼查公司调研。模板：`templates/advice-template.html`

两种建议书均采用 **Tab 多板块导航** 结构，点击 Tab 切换，避免内容堆叠。

#### 7a. 模式 2 建议书结构与 Tab 板块

| Tab | 板块名称 | 内容说明 |
|-----|---------|---------|
| 01 | 岗位匹配度总览 | 总匹配度评分（0-100）+ 匹配项/缺口项清单 |
| 02 | 简历调整说明 | 针对该岗位的侧重调整 + 改写前后对比（2-3 条典型）+ ATS 关键词覆盖 |
| 03 | 目标公司调研 | 天眼查工商信息、社保人数、股权结构、知识产权、关联企业、招聘薪资 |
| 04 | 产品口碑与市场 | 品牌/产品矩阵、淘宝/小红书搜索链接、办公地点地图链接 |
| 05 | 面试准备方向 | 产品理解 + 行业洞察 + 消费者洞察 + 预判面试问题 |
| 06 | 薪资谈判参考 | 岗位薪资范围 + 自身匹配度定位 + 谈判筹码梳理 |
| 07 | 风险与避雷提示 | 岗位风险信号 + 简历风险点 + 天眼查风险数据（失信/处罚/经营异常） |
| 08 | 行动建议 | 投递时机 + 简历微调 + 跟进策略 |

#### 7b. 公司调研数据采集（Tab 03 + 04）

**触发条件**：用户提供的岗位信息中能够提取到目标公司名称。

**采集流程**（详见 `references/company-research-rules.md`）：

1. **天眼查 MCP 授权检查**（首次使用必须执行）：
   - 调用 `RequestAuthorization` 请求天眼查 MCP 授权（service: `trae-remote-official:tianyancha::tianyancha`，scopes: `[]`）
   - 如授权成功 → 走天眼查 MCP 采集流程（步骤 2）
   - 如用户拒绝授权或授权失败 → 自动降级为 WebSearch 模式（步骤 4），并告知用户"天眼查未授权，将使用公开网络搜索，数据精度可能降低"
   - 已授权用户跳过此步，直接进入步骤 2

2. **天眼查 MCP 数据采集**（授权后执行）：
   - 调用 `searchCompany` 获取 companyId
   - 调用 `getCompanyBaseInfo` 获取工商信息（全称、法人、注册资本、成立日期、社保人数、注册地址）
   - 调用 `getCompanyShareholders` 获取股权结构
   - 调用 `getCompanyRisk` 获取风险信息（失信、行政处罚、经营异常）
   - 调用 `getCompanyIpcInfo` 获取知识产权（商标、专利、软著）
   - 调用 `getCompanyRelation` 获取关联企业

3. **互联网公开数据采集**（WebSearch 并行，无论天眼查是否可用都执行）：
   - BOSS直聘/职友集：在招岗位与薪资
   - 小红书：公司避雷、面试经验
   - 淘宝/京东：产品销量与评价
   - 启信宝/企查查页面：补充工商信息交叉验证

4. **WebSearch 降级模式**（天眼查未授权时执行）：
   - 搜索 `"{公司名}" 启信宝 工商信息 社保人数` → 工商基本信息
   - 搜索 `"{公司名}" 天眼查 股东 风险` → 股权与风险
   - 搜索 `"{统一信用代码}"` → 精确匹配工商页面
   - WebFetch 抓取启信宝/天眼查公开页面获取详细数据
   - 在报告中标注"数据来源：公开网络搜索，未经天眼查 MCP 验证"

5. **链接生成**：
   - 高德地图链接：`https://uri.amap.com/search?query={完整地址}`
   - 小红书搜索链接：`https://www.xiaohongshu.com/search_result?keyword={关键词}`
   - 淘宝搜索链接：`https://s.taobao.com/search?q={品牌名}`

6. **数据真实性原则**：
   - 社保缴纳人数（官方数据）优先于 BOSS 自报规模
   - 自报营收必须标注"未经独立验证"
   - 无法在工商系统找到的集团主体，标注"可能为关联企业统称"
   - 天眼查数据与 WebSearch 数据冲突时，以天眼查官方数据为准


#### 7c. 模板占位符

生成时替换以下占位符：

| 占位符 | 替换内容 |
|--------|---------|
| `{{JOB_INFO}}` | 岗位信息 span 标签 |
| `{{MATCH_SECTION}}` | Tab 01 全部内容 |
| `{{RESUME_SECTION}}` | Tab 02 全部内容 |
| `{{COMPANY_SECTION}}` | Tab 03 全部内容（含天眼查数据） |
| `{{PRODUCT_SECTION}}` | Tab 04 全部内容（含产品口碑、地图链接） |
| `{{INTERVIEW_SECTION}}` | Tab 05 全部内容 |
| `{{SALARY_SECTION}}` | Tab 06 全部内容 |
| `{{RISK_SECTION}}` | Tab 07 全部内容（含天眼查风险数据） |
| `{{ACTION_SECTION}}` | Tab 08 全部内容 |

#### 7d. 模式 1 职业规划建议书结构与网页规则

模式 1（通用简历）不针对特定岗位，但**必须生成职业规划建议书**，帮助用户明确职业方向。建议书采用 **Tab 导航**结构，每个职业推荐方向一个 Tab，底部含三方向横向对比。

**详细网页规则见 `references/career-advice-rules.md`，以下为核心内容规范（必须严格遵守）：**

##### 职业推荐方向选取逻辑（1-3 个方向）
- 基于用户履历中的行业经验、专业背景、技能矩阵、实习经历进行匹配分析
- 选取匹配度最高的 1-3 个职业方向，按匹配度从高到低排序
- 每个方向需匹配最新行业薪资数据（通过 WebSearch 采集职友集/BOSS直聘/猎聘/Morgan McKinley 等平台数据）
- 必须标注行业阶段判断（蓝海/成熟期/夕阳产业）和未来 3-5 年前景

##### 网页整体结构
1. **Hero Header**：蓝色渐变背景，标题"求职策略建议书"，副标题（基于个人履历分析的职业推荐与路径规划），4 格 profile-card（姓名、专业、院校、核心经历摘要）
2. **Tab Navigation**：每个职业方向一个 Tab，带推荐优先级 badge（推荐1=红色hot，推荐2=黄色warm，推荐3=绿色cool）
3. **每个 Tab 内含 6 个 Section**（详见下方板块规范）
4. **底部三方向横向对比表**（所有 Tab 之后，不属于任何 Tab）
5. **Footer 数据来源**：所有引用数据必须带编号上标 `sup > a[href="#cite-N"]`，Footer 中 ol 列出完整来源标题+URL

##### 每个 Tab 的 6 个板块内容规范

**板块 01 — 岗位匹配度分析**
- **环形匹配度评分图**：使用 CSS `conic-gradient` 实现环形进度图（非 SVG），分数文字颜色与环形颜色一致。推荐1用蓝色(#2563eb)，推荐2用橙色(#d97706)，推荐3用绿色(#059669)
- **匹配理由清单**：5 条 `li`，每条以 `✓` 开头，**必须结合用户具体经历说明**（不能写通用理由）。例如"字节跳动抖音电商商家运营实习经验直接对口，已掌握商家精细化运营全流程"

**板块 02 — 职业发展路径与薪资水平（核心板块，必须详尽）**
- **5 级晋升路径**：助理/管培生 → 专员 → 主管 → 经理 → 总监，每级必须包含以下完整信息：
  - **圆形序号**（level-num）：1-5
  - **职位名称**（h4）：如"运营助理 / 管培生"
  - **详细职责描述**（desc，3-4 句话）：必须包含 ①入行时间/经验年限 ②**这个阶段具体做什么工作**（如"协助资深运营完成日常后台操作、数据录入、商家沟通等基础工作"）③**关键学习点/能力跃迁点**（如"关键是快速熟悉抖音电商后台、理解GMV拆解逻辑"）④**对用户的个性化建议**（如"你的字节实习经历可让你快速跨过助理期"）。禁止只写一句话概括
  - **标签组**（tags）：2-3 个标签，标注经验年限、角色类型、阶段（如"0-1年经验""基础执行""学习期"）
  - **薪资信息**（level-salary）：月薪范围（amount，加粗着色）+ "月薪"标签 + 年薪范围（annual）
- **ECharts 柱状图**：展示 5 级职位月薪中位数趋势，图表 ID 按方向区分（如 chart-ecommerce、chart-content、chart-brand），**必须懒加载**（Tab 切换时才初始化，避免空白）

**板块 03 — 行业阶段与前景分析**
- **4 张卡片**（2×2 网格布局），每张含：
  - 圆点 + 标题
  - stage-badge 标签（blue=蓝海/mature=成熟/rising=增长/transition=转型）
  - 2-3 段分析文字，关键数据用 `strong` 加粗，带引用上标
- 四张卡片分别为：①行业阶段判断（蓝海/成熟/夕阳，附数据支撑）②未来3-5年前景（增长方向、机会点）③AI对岗位的影响（替代风险+机遇）④核心能力要求演变（短期/中期/长期能力要求，结合用户已有能力做个性化分析）

**板块 04 — 核心技能提升清单（核心板块，必须详尽）**
- **Callout 说明框**：解释标签颜色含义（红色=最大缺口，黄色=需强化，绿色=已有基础）
- **6 项核心技能**，按重要性从高到低排序，每项必须包含：
  - **排名序号**（skill-rank）：圆形，颜色渐变（r1=红→r2=橙→r3=琥珀→r4=柠檬→r5=青→r6=靛蓝）
  - **技能名称 + 缺口标签**（skill-tag）：
    - `gap`（红色背景）：最大缺口 / 关键缺口 / 最大缺口·生存级
    - `boost`（黄色背景）：急需强化 / 需拓展 / 需补齐
    - `advanced`（绿色背景）：已有基础·进阶 / 长期储备
  - **技能描述**（desc，3-4 句话）：必须包含 ①**为什么这项技能重要**（行业趋势、薪资影响，如"未来3年，懂投流的运营比纯运营薪资高30-50%"）②**用户当前的能力现状**（如"你目前具备数据分析能力，但缺乏投流实操经验"）③**能力跃迁的关键点**（如"从'看数据'到'花预算买流量'是运营助理到运营专员的关键跃迁"）
  - **提升路径**（how，灰底框）：3-4 条**具体可执行的步骤**（如"入职后主动申请参与投流工作，学习巨量千川后台操作；自学'投流ROI计算''人群包圈选''出价策略'等核心知识；考取巨量大学认证"）。禁止泛泛而谈
- **技能选取原则**：结合用户已有经历做个性化缺口分析。用户已有能力的技能标绿色（advanced），用户缺失但关键的技能标红色（gap），中间状态标黄色（boost）

**板块 05 — 求职关键因素 — 这个职业最看重什么**
- **Callout 说明框**：点明该职业的"硬通货"是什么（如"电商运营面试，HR 最先看的不是学历，而是你有没有拿得出手的项目数据"）。必须结合职业特点，不能通用化
- **4 张卡片**（2×2 网格布局），按重要性排序：
  - TOP 1 · 硬通货（红色 border-top + 红色 badge）
  - TOP 2 · 加分项（橙色 border-top + 橙色 badge）
  - TOP 3 · 软实力（黄色 border-top + 黄色 badge）
  - TOP 4 · 认知项（绿色 border-top + 绿色 badge）
- 每张卡片含：①为什么重要（why，结合行业特点）②怎么准备（how，灰底框，3-4 条具体步骤）
- **职业差异化要求**：不同职业的"硬通货"必须不同——电商运营看项目数据成果、内容运营看作品集/账号案例、品牌营销看商业洞察与策略思维、设计类看作品集、技术类看代码/GitHub

**板块 06 — 行动建议**
- **Callout 核心判断**：一句话总结该方向的整体判断（用 success/warning 样式区分推荐程度）
- **5 条行动项**（action-list）：①投递目标（具体公司类型+城市）②简历重点突出（结合用户经历的具体数据）③面试准备/差异化竞争 ④薪资谈判（应届生合理薪资区间+谈判筹码）⑤长期规划（几年内做到什么级别，关键转折点是什么）

##### 底部横向对比表
- **8 个维度对比**：匹配度、行业阶段、起薪（应届）、5年薪资天花板、AI替代风险、成长速度、长期天花板、建议优先级
- 每个维度标注最优项（`highlight` 类 + ★ 符号）
- **综合建议 Callout**：给出首份工作推荐方向 + 长期转型方向 + 备选方向的整体策略

##### 数据采集与引用规范
- **薪资数据**：通过 WebSearch 搜索职友集/BOSS直聘/猎聘/Morgan McKinley 薪酬指南，获取各职级月薪范围
- **行业前景**：搜索最新行业分析报告，判断行业阶段（蓝海/成熟/夕阳）
- **AI影响**：搜索AI对该岗位的最新影响分析
- **引用格式**：正文中用 `<sup><a href="#cite-N">[N]</a></sup>` 标注，Footer 中 `<ol>` 列出完整来源（src-title + src-url 链接）
- **所有数据必须标注来源**，Footer 数据来源列表与正文引用一一对应

### Phase 8: 交付

1. 将简历 PDF 保存到用户工作目录
2. 将简历 Word 版保存到用户工作目录
3. （模式 1）将职业规划建议书 HTML 保存到用户工作目录
4. （模式 2）将求职策略建议书 HTML 保存到用户工作目录
5. （三套全生成模式）将 3 套 PDF + 3 套 Word 全部保存到用户工作目录
6. 通过 `computer://` 链接提供所有文件
7. 简要说明：做了哪些调整、匹配度如何、面试准备重点

**交付话术模板：**
> 简历已生成！
> - [简历 PDF] 用于正式投递
> - [简历 Word] 可以自由编辑修改，如果对 PDF 版的排版或措辞不满意，可以在 Word 里调整后保存，也可以告诉我帮你改
> - （模式 1）[职业规划建议书] 1-3 个职业推荐方向，每个方向含职业路径、薪资、行业前景、核心技能提升清单
> - （模式 2）[求职策略建议书] 8 个 Tab 板块，点击顶部导航切换查看，包含天眼查公司调研数据
> - （三套全生成）[经典商务] [现代简约] [创意侧边栏] 三种风格各一套 PDF + Word，可对比挑选

### Phase 9: 满意度确认与迭代引导（新增）

**简历交付后，必须进入满意度确认环节，不能直接结束对话。**

**第一步：通过 `AskUserQuestion` 询问用户满意度（header: "简历满意度"，单选）：**

| 选项 | label | description |
|------|-------|-------------|
| 选项 1 | 满意 | 简历符合预期，无需进一步修改 |
| 选项 2 | 需要调整 | 部分内容或排版需要修改，告诉我哪里不满意 |
| 选项 3 | 大改重做 | 整体方向或风格需要大改，重新调整 |

**第二步：根据用户选择执行后续流程：**

- **选择"满意"** → 进入第三步（Skill 更新引导）
- **选择"需要调整"** → 引导用户具体说明不满意的地方（措辞、排版、经历顺序、内容详略等），针对性修改后重新生成，修改完成后回到第一步再次确认满意度
- **选择"大改重做"** → 询问具体不满意的方向，可能回到 Phase 1 重新选择模式或 Phase 5 重新选择模板，调整后重新生成，完成后回到第一步再次确认满意度

**第三步：Skill 更新引导（用户满意后执行）**

用户表示满意后，通过 `AskUserQuestion` 询问是否将本次偏好沉淀到 Skill 中（header: "更新偏好"，单选）：

| 选项 | label | description |
|------|-------|-------------|
| 选项 1 | 更新 Skill | 将本次简历风格、模板偏好、措辞习惯等记录下来，以后按这种方式生成简历 |
| 选项 2 | 暂不更新 | 本次不记录偏好，保持 Skill 现有默认设置 |

**如用户选择"更新 Skill"：**
1. 总结本次用户的偏好特征（模板风格、经历详略偏好、措辞风格、是否整合经历、是否三套全生成等）
2. 呈现给用户确认："我整理了你的简历偏好如下：[偏好清单]，确认后我会写入 Skill，以后生成简历默认按此方式。"
3. 用户确认后，将偏好追加写入 `~/.trae-cn/memory/user_profile.md` 的 User Preferences 部分（如果涉及跨项目通用偏好）或项目级 memory（如果涉及项目特定偏好）
4. 告知用户："已记录你的简历偏好，下次改简历时会自动按此方式生成。如需调整，随时告诉我。"

**迭代循环规则：**
- 满意度确认是必经环节，不得跳过
- 用户选择"需要调整"或"大改重做"后，修改完成必须再次回到满意度确认，直到用户选择"满意"
- 最多允许 3 轮迭代调整，超过 3 轮仍不满意时，建议用户在 Word 版中自行微调，并提供具体修改建议

## 简历结构顺序

严格按照以下顺序排列板块：

1. **头部**：姓名、求职意向、联系方式、头像
2. **教育背景**（放在最前面）：学校、专业、学历、时间、GPA/排名、相关主修课程、学术荣誉
3. **个人优势**：3-4 条核心亮点摘要
4. **工作/实习经历**：按时间倒序，详略由相关性决定
5. **项目经历**（如有）：课程设计、毕业论文、实践项目
6. **技能证书**：语言、专业技能、工具、核心素质

**教育背景放在最前面的原因：** 本技能客群主要为大学生和应届生，教育背景是他们当前最重要的资质证明，放在最前面能让 HR 第一眼看到学校和专业信息。

## 核心规范

- 价值维度清单与审计规则：详见 `references/value-audit-checklist.md`
- 价值导向改写模式库：详见 `references/rewrite-patterns.md`
- ATS 合规规范与校验清单：详见 `references/ats-compliance.md`
- 岗位关键词提取与匹配规则：详见 `references/job-match-rules.md`
- 公司调研数据采集与展示规范：详见 `references/company-research-rules.md`

## 输出规范

- **简历 PDF**：经典单栏，A4，ATS 友好，数据加粗前置，头像可选（右上角圆形），含装饰性水印和编号标记
- **简历 Word**：可编辑 docx，与 PDF 同风格，供用户细调文字和排版
- **职业规划建议书 HTML**（模式 1）：1-3 个职业推荐方向 Tab 导航，每方向含匹配度分析、职业路径与薪资、行业前景、核心技能提升清单、行动建议。模板：`templates/career-advice-template.html`
- **求职策略建议书 HTML**（模式 2）：8 个 Tab 板块，顶部导航点击切换，含天眼查公司调研数据。模板：`templates/advice-template.html`
- **模板选择**：3 种风格（classic / modern / creative）或三套全生成，默认 classic

## 注意事项

- 语言必须与用户输入语言一致
- 追问必须合并为一次，控制在 3-5 个问题
- **只润色不虚构**：可以对措辞进行价值化提升，但绝不编造数据、项目或经历
- 简历必须价值导向，拒绝流水账
- PDF 文字必须可选可解析
- 交付时提供 `computer://` 链接
- **头像提醒**：在信息采集阶段主动提醒用户上传头像，在追问阶段再次确认
- 头像为可选项，未上传时不影响简历生成，但应明确告知用户有头像效果更好
- 填充模板前必须与用户确认关键事实（学历类型、公司地域、职责边界），避免错误信息写入简历
- **个人信息占位符防护**：生成简历前必须校验手机号、邮箱等必填字段为真实值（非 `xxxx`/`placeholder`/`{{...}}`），发现占位符立即暂停索取真实值，禁止带占位符生成 PDF
- **并行生成**：简历生成与建议书生成应并行执行，先交付简历再交付建议书，缩短用户总等待时间
- PDF 排版参考 Phase 6 的注意事项，遵循版式设计原则
- Word 版供用户细调使用，交付时必须提示用户可以自行修改或让你帮忙改
- **公司调研**：如用户岗位信息含公司名称，必须调用天眼查 MCP 采集数据填充 Tab 03/04；如天眼查不可用，降级为 WebSearch 模式
- **选择框交互**：所有需要用户做选择的环节（模式选择、经历整合、模板选择、满意度确认、Skill 更新），必须使用 `AskUserQuestion` 工具呈现选择框，禁止让用户输入字母/数字/文字回复
- **经历整合机制**：用户经历琐碎零散时，主动触发整合提示，经用户确认后通过价值分析合并提炼强重点经历，整合结果必须经用户确认才能写入简历
- **满意度闭环**：简历交付后必须进入 Phase 9 满意度确认，不得直接结束对话；用户不满意时引导迭代修改，满意后引导是否更新 Skill
- **Skill 更新引导**：用户满意后，主动询问是否将本次偏好写入 Skill，经用户确认后追加写入 memory 文件
- **模式 1 职业规划建议书**：通用简历模式必须生成职业规划建议书，包含 1-3 个职业推荐方向，每方向含 6 个板块（匹配度分析、职业路径与薪资、行业前景、核心技能、求职关键因素、行动建议）+ 底部三方向横向对比表。详细网页规则见 `references/career-advice-rules.md`
- **职业发展路径规范（板块02）**：5 级晋升路径（助理→专员→主管→经理→总监），每级必须详细描述：①经验年限 ②**这个阶段具体做什么工作**（如"协助资深运营完成日常后台操作、数据录入、商家沟通"）③**关键学习点/能力跃迁点**（如"从'做GMV'到'做利润'的转变"）④**对用户的个性化建议**（结合用户经历）。禁止只写一句话概括
- **核心技能提升清单规范（板块04）**：6 项技能按重要性排序，标注缺口类型（gap红/boost黄/advanced绿）。每项技能描述（3-4句）必须包含：①**为什么重要**（行业趋势+薪资影响）②**用户当前能力现状**（结合已有经历）③**能力跃迁关键点**。提升路径必须 3-4 条具体可执行步骤，禁止泛泛而谈
- **求职关键因素规范（板块05）**：4 个求职敲门砖按重要性排序（TOP 1 硬通货/TOP 2 加分项/TOP 3 软实力/TOP 4 认知项）。必须结合职业特点，不能通用化——电商运营看项目数据、内容运营看作品集/账号案例、品牌营销看商业洞察、设计类看作品集、技术类看代码/GitHub
- **ECharts 图表规范**：柱状图展示 5 级月薪中位数，必须懒加载（Tab 切换时初始化），CDN 引用 `https://cdn.jsdelivr.net/npm/echarts@5/dist/echarts.min.js`
- **数据引用规范**：正文中用 `<sup><a href="#cite-N">[N]</a></sup>` 标注，Footer 中列出完整来源标题+URL
